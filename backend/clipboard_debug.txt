--- DEBUG CLIPBOARD HTML ---
ORIGINAL:
<html>
<body>
<!--StartFragment--><html><head></head><body><p>Antigravity IDE supports <strong>portable <code inline="">SKILL.md</code> skills</strong>, so Taste Skill should work there without any special integration. The whole point of this project is that it uses the open <code inline="">SKILL.md</code> format that multiple AI coding agents, including Antigravity, understand. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><p>There are three ways to use it.</p><h3>Method 1 (Recommended): Install with the Skills CLI</h3><p>If you have the Skills CLI installed:</p><pre><code class="language-bash">npx skills add https://github.com/Leonxlnx/taste-skill
</code></pre><p>Or install only the main frontend skill:</p><pre><code class="language-bash">npx skills add https://github.com/Leonxlnx/taste-skill --skill "design-taste-frontend"
</code></pre><p>This creates the appropriate <code inline="">SKILL.md</code> file that compatible agents automatically load. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><hr><h3>Method 2: Copy the SKILL.md manually</h3><p>If Antigravity doesn't support the CLI yet:</p><ol><li><p>Clone the repository:</p></li></ol><pre><code class="language-bash">git clone https://github.com/Leonxlnx/taste-skill.git
</code></pre><ol start="2"><li><p>Open</p></li></ol><pre><code>skills/taste-skill/SKILL.md
</code></pre><ol start="3"><li><p>Copy that file into your project.</p></li></ol><p>A common layout is:</p><pre><code>your-project/
├── .agents/
│   └── skills/
│       └── design-taste-frontend/
│           └── SKILL.md
</code></pre><p>or simply</p><pre><code>your-project/
└── SKILL.md
</code></pre><p>depending on what Antigravity scans. The documentation states the agent simply needs to be able to read the <code inline="">SKILL.md</code> file. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><hr><h3>Method 3: Attach it to the AI conversation</h3><p>If Antigravity lets you attach files to the AI context:</p><ol><li><p>Open <code inline="">SKILL.md</code></p></li><li><p>Drag it into the chat</p></li><li><p>Then prompt normally:</p></li></ol><pre><code>Follow the attached Taste Skill.

Build a modern SaaS landing page for a school management platform using React and Tailwind.
</code></pre><p>The model will follow the rules inside the skill.</p><hr><h2>How to verify it's working</h2><p>Ask the AI:</p><pre><code>What design rules are currently active?
</code></pre><p>or</p><pre><code>Summarize the loaded Taste Skill before generating code.
</code></pre><p>If it mentions things like:</p><ul><li><p>Typography hierarchy</p></li><li><p>Layout variance</p></li><li><p>Motion intensity</p></li><li><p>Visual density</p></li><li><p>Anti-slop rules</p></li></ul><p>then the skill has been loaded.</p><hr><h2>If Antigravity doesn't auto-load skills</h2><p>Some editors don't yet implement automatic skill discovery. In that case, simply prepend your prompt with:</p><pre><code>Read and follow the attached SKILL.md exactly.

Do not ignore any design rules.
</code></pre><p>Crude? Yes. Effective? Also yes. Humans have been reinventing "copy this file into the prompt" since the dawn of LLMs.</p><hr><h2>For Antigravity specifically</h2><p>The Taste Skill documentation explicitly lists <strong>Antigravity</strong> among the compatible AI coding agents that support <code inline="">SKILL.md</code> files, so you do not need any special version of the skill. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><p>If you're referring to the <strong>Antigravity IDE by Alchemyst AI</strong>, I can also show you exactly <strong>where Antigravity stores its project skills</strong> and how to make them load automatically.</p></body></html><!--EndFragment-->
</body>
</html>

TRANSFORMED:
<!--StartFragment--><p>Antigravity IDE supports <strong>portable <code inline="">SKILL.md</code> skills</strong>, so Taste Skill should work there without any special integration. The whole point of this project is that it uses the open <code inline="">SKILL.md</code> format that multiple AI coding agents, including Antigravity, understand. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><p>There are three ways to use it.</p><h3>Method 1 (Recommended): Install with the Skills CLI</h3><p>If you have the Skills CLI installed:</p><pre><code class="language-bash">npx skills add https://github.com/Leonxlnx/taste-skill</code></pre><p>Or install only the main frontend skill:</p><pre><code class="language-bash">npx skills add https://github.com/Leonxlnx/taste-skill --skill "design-taste-frontend"</code></pre><p>This creates the appropriate <code inline="">SKILL.md</code> file that compatible agents automatically load. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><h3>Method 2: Copy the SKILL.md manually</h3><p>If Antigravity doesn't support the CLI yet:</p><ol><li><p>Clone the repository:</p></li></ol><pre><code class="language-bash">git clone https://github.com/Leonxlnx/taste-skill.git</code></pre><ol start="2"><li><p>Open</p></li></ol><pre><code>skills/taste-skill/SKILL.md</code></pre><ol start="3"><li><p>Copy that file into your project.</p></li></ol><p>A common layout is:</p><pre><code>your-project/
├── .agents/
│   └── skills/
│       └── design-taste-frontend/
│           └── SKILL.md</code></pre><p>or simply</p><pre><code>your-project/
└── SKILL.md</code></pre><p>depending on what Antigravity scans. The documentation states the agent simply needs to be able to read the <code inline="">SKILL.md</code> file. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><h3>Method 3: Attach it to the AI conversation</h3><p>If Antigravity lets you attach files to the AI context:</p><ol><li><p>Open <code inline="">SKILL.md</code></p></li><li><p>Drag it into the chat</p></li><li><p>Then prompt normally:</p></li></ol><pre><code>Follow the attached Taste Skill.

Build a modern SaaS landing page for a school management platform using React and Tailwind.</code></pre><p>The model will follow the rules inside the skill.</p><h2>How to verify it's working</h2><p>Ask the AI:</p><pre><code>What design rules are currently active?</code></pre><p>or</p><pre><code>Summarize the loaded Taste Skill before generating code.</code></pre><p>If it mentions things like:</p><ul><li><p>Typography hierarchy</p></li><li><p>Layout variance</p></li><li><p>Motion intensity</p></li><li><p>Visual density</p></li><li><p>Anti-slop rules</p></li></ul><p>then the skill has been loaded.</p><h2>If Antigravity doesn't auto-load skills</h2><p>Some editors don't yet implement automatic skill discovery. In that case, simply prepend your prompt with:</p><pre><code>Read and follow the attached SKILL.md exactly.

Do not ignore any design rules.</code></pre><p>Crude? Yes. Effective? Also yes. Humans have been reinventing "copy this file into the prompt" since the dawn of LLMs.</p><h2>For Antigravity specifically</h2><p>The Taste Skill documentation explicitly lists <strong>Antigravity</strong> among the compatible AI coding agents that support <code inline="">SKILL.md</code> files, so you do not need any special version of the skill. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><p>If you're referring to the <strong>Antigravity IDE by Alchemyst AI</strong>, I can also show you exactly <strong>where Antigravity stores its project skills</strong> and how to make them load automatically.</p>
----------------------------
--- DEBUG CLIPBOARD HTML ---
ORIGINAL:
<html>
<body>
<!--StartFragment--><html><head></head><body><p>Antigravity IDE supports <strong>portable <code inline="">SKILL.md</code> skills</strong>, so Taste Skill should work there without any special integration. The whole point of this project is that it uses the open <code inline="">SKILL.md</code> format that multiple AI coding agents, including Antigravity, understand. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><p>There are three ways to use it.</p><h3>Method 1 (Recommended): Install with the Skills CLI</h3><p>If you have the Skills CLI installed:</p><pre><code class="language-bash">npx skills add https://github.com/Leonxlnx/taste-skill
</code></pre><p>Or install only the main frontend skill:</p><pre><code class="language-bash">npx skills add https://github.com/Leonxlnx/taste-skill --skill "design-taste-frontend"
</code></pre><p>This creates the appropriate <code inline="">SKILL.md</code> file that compatible agents automatically load. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><hr><h3>Method 2: Copy the SKILL.md manually</h3><p>If Antigravity doesn't support the CLI yet:</p><ol><li><p>Clone the repository:</p></li></ol><pre><code class="language-bash">git clone https://github.com/Leonxlnx/taste-skill.git
</code></pre><ol start="2"><li><p>Open</p></li></ol><pre><code>skills/taste-skill/SKILL.md
</code></pre><ol start="3"><li><p>Copy that file into your project.</p></li></ol><p>A common layout is:</p><pre><code>your-project/
├── .agents/
│   └── skills/
│       └── design-taste-frontend/
│           └── SKILL.md
</code></pre><p>or simply</p><pre><code>your-project/
└── SKILL.md
</code></pre><p>depending on what Antigravity scans. The documentation states the agent simply needs to be able to read the <code inline="">SKILL.md</code> file. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><hr><h3>Method 3: Attach it to the AI conversation</h3><p>If Antigravity lets you attach files to the AI context:</p><ol><li><p>Open <code inline="">SKILL.md</code></p></li><li><p>Drag it into the chat</p></li><li><p>Then prompt normally:</p></li></ol><pre><code>Follow the attached Taste Skill.

Build a modern SaaS landing page for a school management platform using React and Tailwind.
</code></pre><p>The model will follow the rules inside the skill.</p><hr><h2>How to verify it's working</h2><p>Ask the AI:</p><pre><code>What design rules are currently active?
</code></pre><p>or</p><pre><code>Summarize the loaded Taste Skill before generating code.
</code></pre><p>If it mentions things like:</p><ul><li><p>Typography hierarchy</p></li><li><p>Layout variance</p></li><li><p>Motion intensity</p></li><li><p>Visual density</p></li><li><p>Anti-slop rules</p></li></ul><p>then the skill has been loaded.</p><hr><h2>If Antigravity doesn't auto-load skills</h2><p>Some editors don't yet implement automatic skill discovery. In that case, simply prepend your prompt with:</p><pre><code>Read and follow the attached SKILL.md exactly.

Do not ignore any design rules.
</code></pre><p>Crude? Yes. Effective? Also yes. Humans have been reinventing "copy this file into the prompt" since the dawn of LLMs.</p><hr><h2>For Antigravity specifically</h2><p>The Taste Skill documentation explicitly lists <strong>Antigravity</strong> among the compatible AI coding agents that support <code inline="">SKILL.md</code> files, so you do not need any special version of the skill. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><p>If you're referring to the <strong>Antigravity IDE by Alchemyst AI</strong>, I can also show you exactly <strong>where Antigravity stores its project skills</strong> and how to make them load automatically.</p></body></html><!--EndFragment-->
</body>
</html>

TRANSFORMED:
<!--StartFragment--><p>Antigravity IDE supports <strong>portable <code inline="">SKILL.md</code> skills</strong>, so Taste Skill should work there without any special integration. The whole point of this project is that it uses the open <code inline="">SKILL.md</code> format that multiple AI coding agents, including Antigravity, understand. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><p>There are three ways to use it.</p><h3>Method 1 (Recommended): Install with the Skills CLI</h3><p>If you have the Skills CLI installed:</p><pre><code class="language-bash">npx skills add https://github.com/Leonxlnx/taste-skill</code></pre><p>Or install only the main frontend skill:</p><pre><code class="language-bash">npx skills add https://github.com/Leonxlnx/taste-skill --skill "design-taste-frontend"</code></pre><p>This creates the appropriate <code inline="">SKILL.md</code> file that compatible agents automatically load. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><h3>Method 2: Copy the SKILL.md manually</h3><p>If Antigravity doesn't support the CLI yet:</p><ol><li><p>Clone the repository:</p></li></ol><pre><code class="language-bash">git clone https://github.com/Leonxlnx/taste-skill.git</code></pre><ol start="2"><li><p>Open</p></li></ol><pre><code>skills/taste-skill/SKILL.md</code></pre><ol start="3"><li><p>Copy that file into your project.</p></li></ol><p>A common layout is:</p><pre><code>your-project/
├── .agents/
│   └── skills/
│       └── design-taste-frontend/
│           └── SKILL.md</code></pre><p>or simply</p><pre><code>your-project/
└── SKILL.md</code></pre><p>depending on what Antigravity scans. The documentation states the agent simply needs to be able to read the <code inline="">SKILL.md</code> file. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><h3>Method 3: Attach it to the AI conversation</h3><p>If Antigravity lets you attach files to the AI context:</p><ol><li><p>Open <code inline="">SKILL.md</code></p></li><li><p>Drag it into the chat</p></li><li><p>Then prompt normally:</p></li></ol><pre><code>Follow the attached Taste Skill.

Build a modern SaaS landing page for a school management platform using React and Tailwind.</code></pre><p>The model will follow the rules inside the skill.</p><h2>How to verify it's working</h2><p>Ask the AI:</p><pre><code>What design rules are currently active?</code></pre><p>or</p><pre><code>Summarize the loaded Taste Skill before generating code.</code></pre><p>If it mentions things like:</p><ul><li><p>Typography hierarchy</p></li><li><p>Layout variance</p></li><li><p>Motion intensity</p></li><li><p>Visual density</p></li><li><p>Anti-slop rules</p></li></ul><p>then the skill has been loaded.</p><h2>If Antigravity doesn't auto-load skills</h2><p>Some editors don't yet implement automatic skill discovery. In that case, simply prepend your prompt with:</p><pre><code>Read and follow the attached SKILL.md exactly.

Do not ignore any design rules.</code></pre><p>Crude? Yes. Effective? Also yes. Humans have been reinventing "copy this file into the prompt" since the dawn of LLMs.</p><h2>For Antigravity specifically</h2><p>The Taste Skill documentation explicitly lists <strong>Antigravity</strong> among the compatible AI coding agents that support <code inline="">SKILL.md</code> files, so you do not need any special version of the skill. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><p>If you're referring to the <strong>Antigravity IDE by Alchemyst AI</strong>, I can also show you exactly <strong>where Antigravity stores its project skills</strong> and how to make them load automatically.</p>
----------------------------
--- DEBUG CLIPBOARD HTML ---
ORIGINAL:
<html>
<body>
<!--StartFragment--><html><head></head><body><p>Antigravity IDE supports <strong>portable <code inline="">SKILL.md</code> skills</strong>, so Taste Skill should work there without any special integration. The whole point of this project is that it uses the open <code inline="">SKILL.md</code> format that multiple AI coding agents, including Antigravity, understand. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><p>There are three ways to use it.</p><h3>Method 1 (Recommended): Install with the Skills CLI</h3><p>If you have the Skills CLI installed:</p><pre><code class="language-bash">npx skills add https://github.com/Leonxlnx/taste-skill
</code></pre><p>Or install only the main frontend skill:</p><pre><code class="language-bash">npx skills add https://github.com/Leonxlnx/taste-skill --skill "design-taste-frontend"
</code></pre><p>This creates the appropriate <code inline="">SKILL.md</code> file that compatible agents automatically load. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><hr><h3>Method 2: Copy the SKILL.md manually</h3><p>If Antigravity doesn't support the CLI yet:</p><ol><li><p>Clone the repository:</p></li></ol><pre><code class="language-bash">git clone https://github.com/Leonxlnx/taste-skill.git
</code></pre><ol start="2"><li><p>Open</p></li></ol><pre><code>skills/taste-skill/SKILL.md
</code></pre><ol start="3"><li><p>Copy that file into your project.</p></li></ol><p>A common layout is:</p><pre><code>your-project/
├── .agents/
│   └── skills/
│       └── design-taste-frontend/
│           └── SKILL.md
</code></pre><p>or simply</p><pre><code>your-project/
└── SKILL.md
</code></pre><p>depending on what Antigravity scans. The documentation states the agent simply needs to be able to read the <code inline="">SKILL.md</code> file. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><hr><h3>Method 3: Attach it to the AI conversation</h3><p>If Antigravity lets you attach files to the AI context:</p><ol><li><p>Open <code inline="">SKILL.md</code></p></li><li><p>Drag it into the chat</p></li><li><p>Then prompt normally:</p></li></ol><pre><code>Follow the attached Taste Skill.

Build a modern SaaS landing page for a school management platform using React and Tailwind.
</code></pre><p>The model will follow the rules inside the skill.</p><hr><h2>How to verify it's working</h2><p>Ask the AI:</p><pre><code>What design rules are currently active?
</code></pre><p>or</p><pre><code>Summarize the loaded Taste Skill before generating code.
</code></pre><p>If it mentions things like:</p><ul><li><p>Typography hierarchy</p></li><li><p>Layout variance</p></li><li><p>Motion intensity</p></li><li><p>Visual density</p></li><li><p>Anti-slop rules</p></li></ul><p>then the skill has been loaded.</p><hr><h2>If Antigravity doesn't auto-load skills</h2><p>Some editors don't yet implement automatic skill discovery. In that case, simply prepend your prompt with:</p><pre><code>Read and follow the attached SKILL.md exactly.

Do not ignore any design rules.
</code></pre><p>Crude? Yes. Effective? Also yes. Humans have been reinventing "copy this file into the prompt" since the dawn of LLMs.</p><hr><h2>For Antigravity specifically</h2><p>The Taste Skill documentation explicitly lists <strong>Antigravity</strong> among the compatible AI coding agents that support <code inline="">SKILL.md</code> files, so you do not need any special version of the skill. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><p>If you're referring to the <strong>Antigravity IDE by Alchemyst AI</strong>, I can also show you exactly <strong>where Antigravity stores its project skills</strong> and how to make them load automatically.</p></body></html><!--EndFragment-->
</body>
</html>

TRANSFORMED:
<!--StartFragment--><p>Antigravity IDE supports <strong>portable <code inline="">SKILL.md</code> skills</strong>, so Taste Skill should work there without any special integration. The whole point of this project is that it uses the open <code inline="">SKILL.md</code> format that multiple AI coding agents, including Antigravity, understand. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><p>There are three ways to use it.</p><h3>Method 1 (Recommended): Install with the Skills CLI</h3><p>If you have the Skills CLI installed:</p><pre><code class="language-bash">npx skills add https://github.com/Leonxlnx/taste-skill</code></pre><p>Or install only the main frontend skill:</p><pre><code class="language-bash">npx skills add https://github.com/Leonxlnx/taste-skill --skill "design-taste-frontend"</code></pre><p>This creates the appropriate <code inline="">SKILL.md</code> file that compatible agents automatically load. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><h3>Method 2: Copy the SKILL.md manually</h3><p>If Antigravity doesn't support the CLI yet:</p><ol><li><p>Clone the repository:</p></li></ol><pre><code class="language-bash">git clone https://github.com/Leonxlnx/taste-skill.git</code></pre><ol start="2"><li><p>Open</p></li></ol><pre><code>skills/taste-skill/SKILL.md</code></pre><ol start="3"><li><p>Copy that file into your project.</p></li></ol><p>A common layout is:</p><pre><code>your-project/
├── .agents/
│   └── skills/
│       └── design-taste-frontend/
│           └── SKILL.md</code></pre><p>or simply</p><pre><code>your-project/
└── SKILL.md</code></pre><p>depending on what Antigravity scans. The documentation states the agent simply needs to be able to read the <code inline="">SKILL.md</code> file. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><h3>Method 3: Attach it to the AI conversation</h3><p>If Antigravity lets you attach files to the AI context:</p><ol><li><p>Open <code inline="">SKILL.md</code></p></li><li><p>Drag it into the chat</p></li><li><p>Then prompt normally:</p></li></ol><pre><code>Follow the attached Taste Skill.

Build a modern SaaS landing page for a school management platform using React and Tailwind.</code></pre><p>The model will follow the rules inside the skill.</p><h2>How to verify it's working</h2><p>Ask the AI:</p><pre><code>What design rules are currently active?</code></pre><p>or</p><pre><code>Summarize the loaded Taste Skill before generating code.</code></pre><p>If it mentions things like:</p><ul><li><p>Typography hierarchy</p></li><li><p>Layout variance</p></li><li><p>Motion intensity</p></li><li><p>Visual density</p></li><li><p>Anti-slop rules</p></li></ul><p>then the skill has been loaded.</p><h2>If Antigravity doesn't auto-load skills</h2><p>Some editors don't yet implement automatic skill discovery. In that case, simply prepend your prompt with:</p><pre><code>Read and follow the attached SKILL.md exactly.

Do not ignore any design rules.</code></pre><p>Crude? Yes. Effective? Also yes. Humans have been reinventing "copy this file into the prompt" since the dawn of LLMs.</p><h2>For Antigravity specifically</h2><p>The Taste Skill documentation explicitly lists <strong>Antigravity</strong> among the compatible AI coding agents that support <code inline="">SKILL.md</code> files, so you do not need any special version of the skill. (<a href="https://www.tasteskill.dev/docs?utm_source=chatgpt.com" title="Documentation | Taste Skill">Taste Skill</a>)</p><p>If you're referring to the <strong>Antigravity IDE by Alchemyst AI</strong>, I can also show you exactly <strong>where Antigravity stores its project skills</strong> and how to make them load automatically.</p>
----------------------------
