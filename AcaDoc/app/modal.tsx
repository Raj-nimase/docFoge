// Unused — kept so expo-router doesn't complain about the route in _layout.
import { View, Text } from 'react-native';
export default function Modal() {
  return <View style={{ flex: 1 }}><Text /></View>;
}
